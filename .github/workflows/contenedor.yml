# Arma la imagen Docker, corre todas las pruebas dentro de ella (SQLite y PostgreSQL con
# row-level security) y la levanta como en producción para comprobar /health.
# Si un paso falla, sus últimas líneas quedan como anotación del run (visibles sin iniciar sesión).
name: Contenedor

on:
  push:
  workflow_dispatch:

env:
  # Publica las últimas líneas de un registro como anotación de error.
  REPORT: tail -n 60 "$1" | sed -e 's/%/%25/g' | awk 'BEGIN{printf "::error::"} {printf "%s%%0A", $0} END{print ""}'

jobs:
  contenedor:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: hub
          POSTGRES_PASSWORD: hub
          POSTGRES_DB: hub_test
        ports: ['5432:5432']
        options: >-
          --health-cmd "pg_isready -U hub -d hub_test"
          --health-interval 5s --health-timeout 5s --health-retries 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Extraer el estado inicial del frontend para las pruebas
        run: |
          node tests/extract-seed.cjs > seed.log 2>&1 || { sh -c "$REPORT" _ seed.log; exit 1; }
          chmod -R a+rwX data
      - name: Construir la imagen
        run: docker build -t ivz-hub . > build.log 2>&1 || { sh -c "$REPORT" _ build.log; exit 1; }
      - name: Pruebas dentro del contenedor
        run: |
          docker run --rm --network host \
            -v "$PWD/tests:/app/tests:ro" -v "$PWD/data:/app/data" \
            -e HUB_ENV=test \
            -e HUB_TEST_ISOLATION_URL=postgresql://hub:hub@localhost:5432/hub_test \
            ivz-hub python -m unittest discover -s tests -v > tests.log 2>&1 \
            || { grep -E '^(FAIL|ERROR):|Error|assert' tests.log | head -40 > failures.log; cat tests.log | tail -n 30 >> failures.log; sh -c "$REPORT" _ failures.log; exit 1; }
          tail -n 5 tests.log
      - name: Arrancar como en producción y comprobar /health
        run: |
          docker run -d --name hub --network host -e PORT=8080 \
            -e DATABASE_URL=postgresql://hub:hub@localhost:5432/hub_test ivz-hub
          for i in $(seq 1 30); do curl -fs http://localhost:8080/health > health.json && break; sleep 2; done
          cat health.json
          if ! { jq -e '.status == "ok" and .database == "postgresql" and .isolation == "row-level-security"' health.json \
              && curl -fs http://localhost:8080/ | grep -q '<html' \
              && curl -fs http://localhost:8080/globe.js > /dev/null \
              && test "$(docker exec hub whoami)" = hub; }; then
            { cat health.json; docker logs hub; } > run.log 2>&1; sh -c "$REPORT" _ run.log; exit 1
          fi
